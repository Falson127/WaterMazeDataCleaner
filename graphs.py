#graphs
from rpy2 import robjects
import pandas as pd

def graphLat(DataFrame):
    treatments = [0,1]
    if type(DataFrame) == pd.DataFrame:
        frame = DataFrame
    

def graphPathlength(DataFrame):
    if type(DataFrame) == pd.DataFrame:
        frame = DataFrame
    return

def graphSpeed(DataFrame):
    if type(DataFrame) == pd.DataFrame:
        frame = DataFrame
    return
