#Stats
import pandas as pd

def RunColumnStatistics(DataFrame):
    if type(DataFrame) != pd.DataFrame:
        return
    inputFrame = DataFrame
    csDataFrame = pd.DataFrame 
    return csDataFrame
